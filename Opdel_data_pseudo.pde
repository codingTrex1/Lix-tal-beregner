/*
 Opdel data filen i strings med et mellemrum

 HashMap<String, ArrayList<Integer>> textfil = new HashMap<String, ArrayList<Integer>>()
    String ord

for (int i=0; i < lines.length; i++) {
    String[] split = lines[i].split(" ");
    String  = split[0];
    String values = split[1];
    
    
    
String = data.txt
 */
